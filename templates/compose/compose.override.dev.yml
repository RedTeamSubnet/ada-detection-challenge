services:
  challenge-api:
    image: redteamsubnet61/rest-aad-challenge:latest
    volumes:
      - "./src/aad_challenge/challenge/scripts/docker-entrypoint.sh:/usr/local/bin/docker-entrypoint.sh"
      - "./src/aad_challenge/challenge/api:/app/rest-aad-challenge/api"
      - "./volumes/.vscode-server:/home/aad-user/.vscode-server"
    # deploy:
    #   replicas: 3
    #   resources:
    #     limits:
    #       cpus: "0.5"
    #       memory: 512M
    # ports: !override
    #   - "10001-10003:${AAD_CHALLENGE_API_PORT:-10001}"
    # command: ["/bin/bash"]
    # command: ["-b", "uvicorn api.main:app --host=0.0.0.0 --port=${AAD_CHALLENGE_API_PORT:-10001} --no-access-log --no-server-header --proxy-headers --forwarded-allow-ips='*' --reload"]
    # healthcheck:
    #   test: ["CMD-SHELL", "curl -f http://localhost:${AAD_CHALLENGE_API_PORT:-10001}/ping"]
    #   start_period: 30s
    #   start_interval: 1s
    #   interval: 5m
    #   timeout: 5s
    #   retries: 3

  miner-commit-api:
    image: myhub/rest-aad-commit:latest
    build:
      context: ./examples/miner_commit
    restart: unless-stopped
    environment:
      TERM: ${TERM:-xterm}
      TZ: ${TZ:-UTC}
    env_file:
      - path: .env
        required: false
    volumes:
      - "./examples/miner_commit/src:/app"
    ports:
      - ${AAD_CHALLENGE_MINER_COMMIT_API_PORT:-10002}:${AAD_CHALLENGE_MINER_COMMIT_API_PORT:-10002}
    command: ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "${AAD_CHALLENGE_MINER_COMMIT_API_PORT:-10002}", "--reload"]
    tty: true
