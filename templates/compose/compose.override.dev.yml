services:
  challenge-api:
    image: redteamsubnet61/ada_detection:latest
    volumes:
      - "./src/ada_challenge/challenge/scripts/docker-entrypoint.sh:/usr/local/bin/docker-entrypoint.sh"
      - "./src/ada_challenge/challenge/api:/app/ada_detection/api"
      - "./volumes/.vscode-server:/home/ada-user/.vscode-server"
    # deploy:
    #   replicas: 3
    #   resources:
    #     limits:
    #       cpus: "0.5"
    #       memory: 512M
    # ports: !override
    #   - "10001-10003:${ADA_CHALLENGE_API_PORT:-10001}"
    # command: ["/bin/bash"]
    # command: ["-b", "uvicorn api.main:app --host=0.0.0.0 --port=${ADA_CHALLENGE_API_PORT:-10001} --no-access-log --no-server-header --proxy-headers --forwarded-allow-ips='*' --reload"]
    # healthcheck:
    #   test: ["CMD-SHELL", "curl -f http://localhost:${ADA_CHALLENGE_API_PORT:-10001}/ping"]
    #   start_period: 30s
    #   start_interval: 1s
    #   interval: 5m
    #   timeout: 5s
    #   retries: 3

  miner-commit-api:
    image: myhub/rest-ada-commit:latest
    build:
      context: ./examples/miner_commit
    restart: unless-stopped
    environment:
      TERM: ${TERM:-xterm}
      TZ: ${TZ:-UTC}
    env_file:
      - path: .env
        required: false
    volumes:
      - "./examples/miner_commit/src:/app"
    ports:
      - ${ADA_CHALLENGE_MINER_COMMIT_API_PORT:-10002}:${ADA_CHALLENGE_MINER_COMMIT_API_PORT:-10002}
    command: ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "${ADA_CHALLENGE_MINER_COMMIT_API_PORT:-10002}", "--reload"]
    tty: true
